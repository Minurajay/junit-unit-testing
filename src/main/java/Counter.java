public class Counter {
    private int count = 0;

    // Method to increment the counter
    public int increment() {
        return ++count;
    }

    // Getter for the count
    public int getCount() {
        return count;
    }
}
